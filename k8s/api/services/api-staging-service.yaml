---
# Service
apiVersion: v1
kind: Service
metadata:
  name: api-service
  namespace: staging
spec:
  selector:
    app: api
  ports:
  - port: 8081
    targetPort: 80
# - port: 443
#   targetPort: 443
    nodePort: 30001  # FIXME: Force Node port ...
  type: NodePort
  # type: LoadBalancer