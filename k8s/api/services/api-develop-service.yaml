---
# Service
apiVersion: v1
kind: Service
metadata:
  name: api-service
  namespace: develop
spec:
  selector:
    app: api
  ports:
  - port: 8080
    targetPort: 80
# - port: 443
#   targetPort: 443
    nodePort: 30000  # FIXME: Force Node port ...
  type: NodePort
  # type: LoadBalancer